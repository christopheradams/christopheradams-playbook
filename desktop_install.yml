---
- hosts: localhost
  gather_facts: no
  connection: local
  become: yes
  roles:
    - common
    - ruby
    - node
    - elixir
    - haskell
    - git-annex
    - desktop
    - docker
  vars:
    ubuntu_version: bionic
