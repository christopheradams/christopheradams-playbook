---
- hosts: localhost
  gather_facts: no
  connection: local
  become: yes
  roles:
    - common
    - ruby
    - node
    - elixir
    - desktop
    - docker
    - role: ipfs
      become: no
  vars:
    ubuntu_version: bionic
